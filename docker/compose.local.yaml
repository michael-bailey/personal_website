name: "Local personal website"

services:
  postgres:
    image: postgres:16
    container_name: local-spring-blog-postgres
    environment:
      POSTGRES_DB: blogdb
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data

  spring-blog-app:
    build:
      context: ../
      dockerfile: ./backend/docker/Dockerfile
    container_name: local-spring-blog-app
    ports:
      - "8484:8080"
    depends_on:
      - postgres
    environment:
      USERNAME: ${USERNAME}
      PASSWORD: ${PASSWORD}
      POSTGRES_URL: "jdbc:postgresql://postgres:5432/blogdb"
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
      ALLOWED_HOSTS: "http://localhost:8485,http://localhost:8484"
      GITHUB_ACCESS_TOKEN: ${GITHUB_ACCESS_TOKEN}

  next-blog-app:
    build:
      context: ../
      dockerfile: ./frontend/docker/Dockerfile
    container_name: local-next-blog-app
    depends_on:
      - spring-blog-app
    ports:
      - "8485:3000"
    environment:
      HOST: 0.0.0.0
      NEXT_PUBLIC_API_HOSTNAME: http://spring-blog-app
      API_HOSTNAME: http://spring-blog-app

  nginx:
    image: nginx:latest
    container_name: local-spring-blog-nginx
    ports:
      - "80:80"
    volumes:
      - ../default.local.conf:/etc/nginx/conf.d/default.conf

volumes:
  pgdata:
    name: "spring-blog-pgdata"
