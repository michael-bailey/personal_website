services:
  postgres:
    image: postgres:16
    container_name: spring-blog-db
    user: 1001:1001
    environment:
      POSTGRES_DB: blogdb
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ./data:/var/lib/postgresql/data

  spring-blog-app:
    image: ghcr.io/michael-bailey/personal_website-backend:latest
    container_name: spring-blog-app
    depends_on:
      - postgres
    environment:
      POSTGRES_URL: "jdbc:postgresql://postgres:5432/blogdb"
    env_file:
      - .env

  nextjs-blog-app:
    image: ghcr.io/michael-bailey/personal_website-frontend:latest
    container_name: next-blog-app
    environment:
      HOST: 0.0.0.0
      NEXT_PUBLIC_API_HOSTNAME: http://spring-blog-app
      API_HOSTNAME: http://spring-blog-app

  nginx:
    image: nginx:latest
    container_name: local-spring-blog-nginx
    depends_on:
      - spring-blog-app
      - nextjs-blog-app
    ports:
      - "81:81"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf